query findAllSubscribers {
  instances {
    entities(
      type: "mqtt_subscriber"
    ) {
      properties {
        name
        value
        type {
          dataType
          socketType
        }
      }
    }
  }
}
